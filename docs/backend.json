
{
  "entities": {
    "Organizer": {
      "title": "Organizador",
      "description": "Informações do promotor do evento",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "avatar": { "type": "string" },
        "email": { "type": "string" },
        "isVerified": { "type": "boolean" },
        "totalEvents": { "type": "number" }
      },
      "required": ["name", "email"]
    },
    "Event": {
      "title": "Evento",
      "description": "Informações do evento para divulgação",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "location": { "type": "string" },
        "city": { "type": "string" },
        "type": { "type": "string", "enum": ["Público", "Privado", "Governo"] },
        "status": { "type": "string", "enum": ["A fazer", "Em progresso", "Concluído"] },
        "image": { "type": "string" },
        "organizerId": { "type": "string", "description": "UID do usuário que criou o evento" },
        "organizer": { "$ref": "#/backend/entities/Organizer" }
      },
      "required": ["title", "date", "location", "organizerId"]
    },
    "Registration": {
      "title": "Participação",
      "description": "Registro de interesse de um usuário em um evento",
      "type": "object",
      "properties": {
        "eventId": { "type": "string" },
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "userEmail": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["eventId", "userId"]
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/events/{eventId}": {
      "schema": "Event",
      "description": "Coleção global de eventos para divulgação"
    },
    "/users/{userId}": {
      "schema": "Organizer",
      "description": "Perfis dos organizadores cadastrados"
    },
    "/registrations/{regId}": {
      "schema": "Registration",
      "description": "Interesses registrados nos eventos"
    }
  }
}
