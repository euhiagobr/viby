// ARQUIVO DEPRECADO APÓS MIGRAÇÃO
// As regras ativas estão em firestore.rules utilizando caminhos dinâmicos $(database)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}