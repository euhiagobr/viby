rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEPLOY VIBY - Versão: 1.0.5
    // REGRAS PÚBLICAS TOTAIS PARA A PLATAFORMA VIBY
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}