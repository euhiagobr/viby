// ARQUIVO REMOVIDO EM AUDITORIA DE INFRAESTRUTURA
// A plataforma utiliza exclusivamente firestore.rules na raiz.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
