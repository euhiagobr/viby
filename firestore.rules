rules_version = '2';
service cloud.firestore {
  match /databases/(default)/documents {
    // Regras do sistema da ONG - Mantidas intactas
    function isAuthenticated() {
      return request.auth != null;
    }
    
    match /{allPaths=**} {
      allow read, write: if isAuthenticated();
    }
  }
}