/**
 * @fileOverview Rota neutralizada para evitar conflitos de Route Segment.
 * O Next.js exige apenas um dynamic segment no mesmo nível de [username].
 */
export default function NeutralizedPage() {
  return null;
}
