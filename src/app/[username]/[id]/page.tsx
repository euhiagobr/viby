/**
 * @fileOverview ROTA DESATIVADA PARA RESOLVER CONFLITO DE ROTEAMENTO.
 * 
 * O Next.js não permite dois parâmetros dinâmicos diferentes ('id' e 'slug') no mesmo nível.
 * Toda a lógica de carregamento de eventos (por ID ou por Slug) foi consolidada em:
 * src/app/[username]/[slug]/page.tsx
 * 
 * Este arquivo foi esvaziado de exportações para que o Next.js ignore este diretório como uma rota ativa.
 */
