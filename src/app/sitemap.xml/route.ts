/**
 * ARQUIVO REMOVIDO DA ÁRVORE ATIVA
 * Esta implementação via Route Handler foi consolidada no arquivo idiomático /src/app/sitemap.ts.
 * Para evitar conflitos de rota no Next.js App Router, este arquivo foi neutralizado.
 */
export async function GET() {
  return new Response(null, { status: 404 });
}
