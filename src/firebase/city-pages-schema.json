{
  "collection": "events",
  "description": "Campos para suporte a páginas de cidades e SEO local.",
  "fields": {
    "citySlug": "string (ex: porto-alegre)",
    "stateSlug": "string (ex: rs)",
    "countrySlug": "string (ex: br)",
    "regionSlug": "string (ex: br-rs)",
    "city": "string",
    "state": "string"
  }
}
