{
  "collection": "events",
  "description": "Estrutura de suporte para roteamento global de eventos (Viby v2).",
  "fields": {
    "slug": {
      "type": "string",
      "description": "Slug textual único em toda a coleção de eventos.",
      "constraints": "Obrigatório, indexado, lowercase, sem caracteres especiais."
    },
    "organizationId": {
      "type": "string",
      "description": "Referência forte para a coleção 'organizations'."
    },
    "organizer": {
      "type": "map",
      "description": "Dados desnormalizados para evitar joins custosos na renderização de cards.",
      "properties": {
        "id": "string",
        "name": "string",
        "username": "string",
        "avatar": "url string"
      }
    }
  },
  "indexes_required": [
    {
      "collectionId": "events",
      "fields": [
        { "fieldPath": "slug", "order": "ASCENDING" },
        { "fieldPath": "status", "order": "ASCENDING" }
      ]
    }
  ]
}