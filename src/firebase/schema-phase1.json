
{
  "collections": {
    "events": {
      "new_fields": {
        "type": "string ('interno' | 'externo' | 'divulgacao') - default: 'interno'",
        "externalUrl": "string (URL validada para eventos externos)",
        "tags": "array of strings",
        "viewsCount": "number (contador incremental)",
        "interestedCount": "number (contador incremental)",
        "goingCount": "number (contador incremental)",
        "sharesCount": "number (contador incremental)",
        "searchKeywords": "array of strings (para busca aprimorada sem acento)"
      }
    }
  }
}
