{
  "collections": {
    "users": {
      "id": "UID do Auth",
      "fields": {
        "username": "string (único global)",
        "displayName": "string",
        "email": "string",
        "avatar": "url",
        "bio": "string",
        "cpf": "string (AES CTR encrypted)",
        "gender": "masculino | feminino | etc",
        "birthDate": "string (YYYY-MM-DD)",
        "city": "string",
        "state": "string",
        "country": "string",
        "plan": "free | pro | top",
        "preferredCurrency": "BRL | USD | EUR",
        "language": "pt-BR | en-US",
        "createdAt": "timestamp"
      }
    },
    "organizations": {
      "id": "auto-id",
      "fields": {
        "username": "string (único global)",
        "name": "string",
        "slug": "string",
        "bio": "string",
        "type": "produtora | ong | artista | etc",
        "avatar": "url",
        "verified": "boolean",
        "createdBy": "UID",
        "createdAt": "timestamp"
      }
    },
    "events": {
      "id": "auto-id",
      "fields": {
        "organizationId": "string (ref:organizations)",
        "title": "string",
        "description": "string",
        "currency": "string (BRL | USD | EUR)",
        "ticketMode": "none | free | paid_single | batches | sector_batches",
        "mapMode": "none | setores | assentos | mesas",
        "possuiMapa": "boolean",
        "capacidadeTotal": "number",
        "batches": "array of objects { id, name, startDate, endDate, capacity, ticketTypes: [{ id, name, price, quantity, poolId, requiresProof }] }",
        "status": "string",
        "createdAt": "timestamp"
      }
    },
    "registrations": {
      "fields": {
        "eventId": "string",
        "userId": "string",
        "ticketCode": "string",
        "currency": "string (BRL | USD | EUR)",
        "price": "number (valor pago na moeda do evento)",
        "paymentStatus": "string",
        "checkedIn": "boolean",
        "batchId": "string",
        "ticketTypeId": "string"
      }
    }
  }
}