{
  "collections": {
    "users": {
      "id": "UID do Auth",
      "fields": {
        "username": "string (único global)",
        "displayName": "string",
        "email": "string",
        "avatar": "url",
        "bio": "string",
        "cpf": "string (AES CTR encrypted)",
        "gender": "masculino | feminino | etc",
        "birthDate": "string (YYYY-MM-DD)",
        "city": "string",
        "state": "string",
        "country": "string",
        "plan": "free | pro | top",
        "createdAt": "timestamp"
      }
    },
    "organizations": {
      "id": "auto-id",
      "fields": {
        "username": "string (único global)",
        "name": "string",
        "slug": "string",
        "bio": "string",
        "type": "produtora | ong | artista | etc",
        "avatar": "url",
        "verified": "boolean",
        "createdBy": "UID",
        "createdAt": "timestamp"
      }
    },
    "organization_members": {
      "id": "UID do Usuário",
      "path": "organizations/{orgId}/members/{userId}",
      "fields": {
        "role": "owner | admin | editor | finance | checkin",
        "joinedAt": "timestamp"
      }
    },
    "events": {
      "id": "auto-id",
      "fields": {
        "organizationId": "string (ref:organizations)",
        "title": "string",
        "slug": "string",
        "status": "published | draft",
        "visibility": "public | private",
        "createdAt": "timestamp"
      }
    },
    "usernames": {
      "id": "username (lower)",
      "fields": {
        "uid": "ID do dono (user ou org)",
        "type": "user | organization"
      }
    }
  }
}
