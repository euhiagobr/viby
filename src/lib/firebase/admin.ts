
/**
 * @fileOverview ARQUIVO DEPRECADO.
 * O uso do Firebase Admin SDK foi removido para evitar erros de autenticação em ambiente de prototipagem.
 * Utilize exclusivamente o Client SDK disponível em @/firebase.
 */
export const isStoreAdmin = false;
