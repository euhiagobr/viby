rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // REGRAS PÚBLICAS TOTAIS (CONFORME SOLICITADO)
    // Permite leitura e escrita para qualquer pessoa em todos os arquivos e buckets.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}