
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // DEPLOY VIBY - Versão: 1.3.0 (Bucket Viby Especial)
    // DATA: 2024-05-21
    // ACESSO PÚBLICO TOTAL PARA QUALQUER BUCKET (VIBY E PADRÃO)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
