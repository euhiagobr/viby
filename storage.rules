
rules_version = '2';
service firebase.storage {
  match /b/ong-desafios-3942a.firebasestorage.app/o {
    // REGRAS ONG - Versão: 2.0.0
    // Isolamento para o bucket default da ONG
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
