rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // DEPLOY VIBY - Versão: 1.0.5
    // REGRAS PÚBLICAS TOTAIS PARA STORAGE
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}