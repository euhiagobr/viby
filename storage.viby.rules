
rules_version = '2';
service firebase.storage {
  match /b/viby/o {
    // REGRAS VIBY - Versão: 1.0.0 (Acesso Público Plataforma)
    // Bucket destinado a arquivos públicos da plataforma Viby Club
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
