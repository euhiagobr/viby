rules_version = '2';
service firebase.storage {
  // REGRAS STORAGE VIBY - Versão: 2.0.0
  // Bucket: viby
  match /b/viby/o {
    
    // Assets do site (logos e ícones)
    match /site_assets/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null; // Admin é verificado no código, mas aqui permitimos auth
    }

    // Avatares de perfil
    match /profiles/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Imagens de eventos
    match /events/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Regra padrão
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}